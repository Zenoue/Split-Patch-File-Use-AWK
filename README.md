# Split-Patch-File-Use-AWK
## Require:
```
sudo apt install mawk
```
## Usage:
Download `spl-p` and run in folder contain `spl-p`
```
chmod +x spt-p
sudo mv spl-p /usr/local/bin/
```
Run
```
spl-p <patch_file>
```
